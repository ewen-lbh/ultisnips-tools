a, textarea, input, button
    color: inherit
    font-family: inherit
    font-size: inherit
    resize: vertical

textarea, input
    &:hover
        cursor: text

button, a, input[type=radio] + label, input[type=checkbox] + label
    &:hover
        cursor: pointer
